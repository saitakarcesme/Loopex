# B13 — supplied screenshot correction

Source: 61c04dc, alpha13. Binding reference is the user's Codex screenshot, not a generic interpretation. No old source/CSS/assets transferred.

## Implemented and observed
- Bottom composer with project strip, central outlined terminal emblem and project-aware title, four horizontal colored suggestion cards.
- Neutral surface colors, border radii, typography/spacing calibration; sidebar brand/search/history and actual persistent project pinning.
- Model/effort controls styled without native select border. Permission and attachment controls inside composer.
- One user-message background; readable exact host tool names; fixed composer geometry between empty and transcript states. Active queue moved into existing strip in source.
- Native project picker selected the actual AkorithNext repository. Display-name rename and pin are real metadata changes. Pin and completed turn survived normal Quit/reopen.
- Actual Astra read-only package.json request completed with correct name/version. Tool output inspected. Final package renders that persisted turn with “Read file”. Files panel opened with real project files; layout remained usable.
- Suggestion card filled draft without sending. Model picker listed actual configured subscriptions/local model; Escape returned focus.
- 325 tests passed, TypeScript passed, ad-hoc package deep/strict signature verified. Receipt binds final app.asar and screenshots.

## Limits / failed or incomplete checks
- This is a composition correction, not full exact Codex parity. Sidebar navigation/account row, exact glyphs and composer controls still differ. User profile keeps saved width253; new default265. Existing user width was not silently overwritten.
- Native drag attempts did not produce a verifiable width change. Resize lifecycle unit tests pass but native drag/persistence acceptance remains OPEN; no success inferred from attempted gestures.
- Latest active queue positioning is source-tested/layout-reviewed, not independently re-observed in a new live turn after final package. Actual earlier run Starting/Working/Stop and completion are retained separately.
- Static screenshots do not prove equivalent animation durations/easing. No exact motion claim.
- Computer panel reports Accessibility and Screen recording required; capture disabled. No permission was granted and no computer-use readiness claim.
- verify:delivery remains blocked: source changes invalidate 12 historical B12 verifications and 89 other requirements remain pending. Historical evidence preserved, not relabeled current.
- Native Stop hook trust/load/event is unproven. Git hook/source-evidence guard is distinct. No recurring automation.

## Evidence and resources
B13 proof directory contains final-new-task.jpg, final-transcript.jpg, final-right-panel.jpg, reopened-final.jpg, full AX snapshots, final-package-receipt.json, tests/package/gate logs. Earlier first-native-layout.png is JPEG despite extension and is BEFORE final corrections.
Only B13 app and its normal helper/provider processes remained at resource check. Prior B12 closed before packaging; no new browser tabs, fixture servers or recorders opened in this correction. No push; existing installed app/data unchanged. Final independent opinion is recorded separately and did not dispatch product changes.
